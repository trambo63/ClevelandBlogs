@model IEnumerable<ClevelandBlogs.Models.AddModels.AddListItem>

@{
    ViewBag.Title = "Index";
}

<h2>AddsAdmin</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@if (TempData.ContainsKey("SaveResult"))
{
    <div class="alert alert-success fade in" role="alert">

        <button type="button" class="close" data-dismiss="alert">
            <span>&times;</span>
        </button>
        @TempData["SaveResult"]
    </div>
}
@foreach (var item in Model)
{
    <section class="post-body">
        <div class="row">
            <div class="col-lg-10">
                <div class="row">
                    <div class="col-sm-10">
                        <h5>@Html.DisplayFor(modelItem => item.AddId)</h5>
                        <h5>@Html.DisplayFor(modelItem => item.Title)</h5>
                        <h5>@Html.DisplayFor(modelItem => item.Content)</h5>
                        <h5>@Html.DisplayFor(modelItem => item.CreatedUtc)</h5>
                    </div>
                </div>
            </div>
            <aside class="col-md-1">
                <div class="row">
                    <div class"col-sm-4">
                        <a>
                            @Html.ActionLink("Edit", "Edit", new { id = item.AddId })
                        </a>
                    </div>
                    <div class"col-sm-4">
                        <a>
                            @Html.ActionLink("Details", "Details", new { id = item.AddId })
                        </a>
                    </div>
                    <div>
                        <a class="social-link py-4 w-100">
                            @Html.ActionLink("Delete", "Delete", new { id = item.AddId })
                        </a>
                    </div>
                </div>
            </aside>
        </div>
    </section>
}

